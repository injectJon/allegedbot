// export function isGod(message) {
//   // TODO: replace with is jon check.
//   const injectJon = '23807572';

//   return (message === injectJon);
// }

// export function isAdmin(server, message) {
//   return (isGod(message) || server.admins.includes(message));
// }
